@model IEnumerable<Warsztat.Models.Repairs>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FinishDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cars.Brand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Services.ServiceName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FinishDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cars.Brand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Services.ServiceName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_repair }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_repair }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_repair })
        </td>
    </tr>
}

</table>
